Options -Indexes

RewriteEngine On

# Route all /api/* requests to index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^api/(.*)$ index.php [QSA,L]

# Deny direct access to src/ and config/
RewriteRule ^(src|config)/ - [F,L]
